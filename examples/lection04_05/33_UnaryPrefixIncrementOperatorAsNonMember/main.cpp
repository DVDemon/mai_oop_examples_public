#include <iostream>
#include "point.h"

int main()
{
    // Создаем точку с координатами (10, 10)
    Point p1(10, 10);
    std::cout << "p1 : " << p1 << std::endl;

    // Демонстрация явного вызова friend-функции operator++
    // Это эквивалентно ++p1, но показывает, что operator++ - это внешняя функция
    operator++(p1);

    std::cout << "p1 : " << p1 << std::endl;  // Результат: (11, 11)

    // Использование обычного синтаксиса префиксного оператора ++
    // Компилятор автоматически преобразует ++p1 в вызов operator++(p1)
    for (size_t i{}; i < 20; ++i) {
        ++p1;  // Инкрементируем обе координаты точки
        std::cout << "p1 : " << p1 << std::endl;
    }

    return 0;
}