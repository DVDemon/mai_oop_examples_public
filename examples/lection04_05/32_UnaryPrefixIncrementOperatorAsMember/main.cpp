#include <iostream>
#include "point.h"

int main()
{
    // Создаем точку с координатами (10, 10)
    Point p1(10, 10);
    std::cout << "p1 : " << p1 << std::endl;

    // Демонстрация двух эквивалентных способов вызова префиксного оператора ++
    // Обычный синтаксис: ++p1;
    // Явный вызов метода:
    p1.operator++();

    std::cout << "p1 : " << p1 << std::endl;  // Результат: (11, 11)

    // Использование префиксного оператора инкремента в цикле
    // Префиксный оператор ++ изменяет объект до использования его значения
    for (size_t i{}; i < 20; ++i) {
        ++p1;  // Инкрементируем обе координаты точки
        std::cout << "p1 : " << p1 << std::endl;
    }

    return 0;
}