#ifndef DOG_H
#define DOG_H

#include <string>
#include <iostream>

// ============================================================================
// КЛАСС СОБАКИ ДЛЯ ДЕМОНСТРАЦИИ УМНЫХ УКАЗАТЕЛЕЙ
// ============================================================================

class Dog
{
public:
    // ========================================================================
    // КОНСТРУКТОРЫ И ДЕСТРУКТОР
    // ========================================================================
    
    // Конструктор с именем (explicit для предотвращения неявных преобразований)
    explicit Dog(std::string name_param);
    
    // Конструктор по умолчанию
    Dog() = default;
    
    // Деструктор
    ~Dog();
    
    // ========================================================================
    // МЕТОДЫ ДОСТУПА К ДАННЫМ
    // ========================================================================
    
    // Печать информации о собаке
    void print_dog() const {
        std::cout << "Printing dog : " << dog_name << std::endl;
    }
    
    // Получение имени собаки
    std::string get_name() const {
        return dog_name;
    }
    
    // Установка имени собаки
    void set_dog_name(const std::string& name) {
        dog_name = name;
    }
    
private:
    // ========================================================================
    // ЧЛЕНЫ ДАННЫХ
    // ========================================================================
    
    std::string dog_name {"Puffy"};  // Имя собаки с значением по умолчанию
};

#endif // DOG_H
